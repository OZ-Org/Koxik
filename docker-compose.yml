version: "3.9"

services:
  postgres:
    image: postgres:15
    env_file:
      - .env
    volumes:
      - pgdata:/var/lib/postgresql/data

  koxik-bot:
    build: .
    env_file:
      - .env
    depends_on:
      - postgres
    command: bun --env-file=.env.dev src/index.ts

volumes:
  pgdata:
